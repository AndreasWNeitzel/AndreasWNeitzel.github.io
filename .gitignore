_site
.bundle
.DS_store
.idea
.jekyll-cache
.jekyll-metadata
.ruby-version
.sass-cache
.tweet-cache
.venv
assets/libs/
assets/rendercv/rendercv_output/*.typ
Gemfile.lock
node_modules/
vendor

# Local tooling and editor artifacts
.claude/
.gemini/
CLAUDE.local.md

# Stellar Explorer data file lives in the separate stellar-explorer-data repo;
# never commit the 152 MB HDF5 to the site repo.
*.h5

# Windows NTFS alternate-data-stream artifacts (appear when copying from Windows)
*:Zone.Identifier